import React from 'react';

import './Button.scss';

const Button = () => {
    return (
        <button className='button button-animated'>
            press me!
        </button>
    );
};

export default Button ;
